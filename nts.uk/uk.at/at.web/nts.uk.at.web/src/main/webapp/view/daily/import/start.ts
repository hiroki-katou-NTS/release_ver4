module nts.uk.at.view.daily.importing {
    __viewContext.ready(function() {
//        let data = nts.uk.ui.windows.getShared('eventData');
        __viewContext.bind(new viewmodel.ScreenModel());
    });
}